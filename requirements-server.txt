# Packages the /predict server needs, pinned to the versions the model was
# trained and tested with (Python 3.12). tensorflow-cpu is the same TensorFlow
# without GPU support, which Cloud Run does not use.
tensorflow-cpu==2.21.0
keras==3.15.1
numpy==2.5.3
h5py==3.14.0
pillow==12.3.0
flask==3.1.3
gunicorn==26.2.0
