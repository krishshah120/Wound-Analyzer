# Packages the Cloud Run /predict server needs (Python 3.12). It runs the
# TensorFlow Lite copy of the model with LiteRT, so TensorFlow itself is not
# installed - that import made cold starts take ~25 s. Training, evaluation and
# export_tflite.py still need TensorFlow 2.21.0 / Keras 3.15.1.
ai-edge-litert==2.2.0
numpy==2.5.3
pillow==12.3.0
flask==3.1.3
gunicorn==26.2.0
